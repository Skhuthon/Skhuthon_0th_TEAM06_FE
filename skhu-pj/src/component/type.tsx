export type Upload = {
  id: number;
  like: number;
  post_id: number;
  title: string;
};

export default Upload

export type Photo = {
  id: string;
 title: string;
  userName: string;
  audioPath: string;
  imagePath: string;
  like_count: number;
}

